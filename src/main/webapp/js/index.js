window.addEventListener("DOMContentLoaded", function(){

    let modeloMujerBlanco = document.getElementById("modeloMujerBlanco");

    function actualizarColor() {
        // Obtener el elemento select
        var colorList = document.getElementById("colorList");
        // Obtener el valor seleccionado en el elemento select
        var colorSeleccionado = colorList.value;
        // Actualizar el valor del input type="color" con el color seleccionado
        document.getElementById("colorPicker").value = colorSeleccionado;
    }

   

    














})